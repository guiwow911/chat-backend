# 简聊真实部署版

这是 Node.js + Express + Socket.IO + PostgreSQL 的多人聊天版本。

Render:
- Root Directory: 留空
- Build Command: npm install
- Start Command: npm start
- 配置 DATABASE_URL（PostgreSQL）
- 配置 JWT_SECRET
- 不要只上传 index.html；package.json 必须位于项目根目录。
